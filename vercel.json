{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install && npm install --save-dev typescript @types/react @types/node && npm install framer-motion resend"
      }
    }
  ]
}
